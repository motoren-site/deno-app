apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deno-ingress
  namespace: apps
spec:
  ingressClassName: nginx
  rules:
    - host: app.ostepopp.site
      https:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: deno-app
                port:
                  number: 80
